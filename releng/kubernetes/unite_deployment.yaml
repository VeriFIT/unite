# This file is generated by deployments-ci-template project.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: unite-deployment
  labels:
    app: unite
spec:
  replicas: 1
  selector:
    matchLabels:
      app: unite
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: unite
    spec:
      hostname: unite
      containers:
        - name: unite
          image: 
          ports:
            - containerPort: 8080
              hostPort: 9080
              protocol: TCP
            - containerPort: 8081
              hostPort: 9081
              protocol: TCP
          env:
            - name: UNITE_ANALYSIS_HOST
              value: http://verifit1.fit.vutbr.cz
            - name: UNITE_ANALYSIS_PORT
              value: "31080"
            - name: UNITE_COMPILATION_HOST
              value: http://verifit1.fit.vutbr.cz
            - name: UNITE_COMPILATION_PORT
              value: "31081"
      imagePullSecrets:
        - name: registry-credentials
      restartPolicy: Always