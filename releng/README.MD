This is a pregenerated directory. 
Please, do not modify `deployments.json`, as it is containing information about the generated deployment configuration, which is used directly in pipeline.

Customize deployment triggers and versioning in `release-ci.yaml` file.
